import React from 'react'
import Base from "../core/Base"

const Profile = () => {
    return (
        <Base title="Profile">
            <h2>Profile Page</h2>
        </Base>
    )
}

export default Profile